# Loaded by `vite --mode cloud` (npm run dev:cloud). Forces emulators
# off so the dev server is reliable regardless of any stale
# VITE_USE_EMULATORS value still in a developer's .env.local.
VITE_USE_EMULATORS=false
