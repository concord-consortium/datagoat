# Loaded by `vite build` (mode = production). Forces emulators off so
# production bundles are immune to a stale VITE_USE_EMULATORS=true
# left over in any developer's .env.local.
VITE_USE_EMULATORS=false
