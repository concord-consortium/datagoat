// No migrations registered yet (every UserProfile is at v1). When a v1 -> v2
// migration is needed, register it here via registerMigration('userProfile', 1, fn)
// and add a v2 fixture to userProfile.fixtures.ts.
export const CURRENT_USER_PROFILE_VERSION = 1;
