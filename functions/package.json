{
  "name": "functions",
  "private": true,
  "version": "0.1.0",
  "description": "Cloud Functions for DataGOAT",
  "main": "lib/index.js",
  "type": "commonjs",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "vitest run",
    "smoke:blocked-no-email": "node scripts/smoke-blocked-no-email.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vitest": "^4.1.5"
  }
}
